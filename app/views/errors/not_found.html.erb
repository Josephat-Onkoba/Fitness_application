<div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full text-center">
    <div class="mb-6">
      <h1 class="text-9xl font-bold text-blue-600">404</h1>
      <h2 class="text-2xl font-semibold text-gray-800 mt-4">Page Not Found</h2>
      <p class="text-gray-600 mt-2">The page you're looking for doesn't exist or has been moved.</p>
    </div>
    <div class="mt-8">
      <%= link_to "Back to Home", root_path, class: "inline-block bg-blue-600 text-white px-6 py-3 rounded-md font-medium hover:bg-blue-700 transition-colors" %>
    </div>
    <div class="mt-6">
      <p class="text-gray-500">Looking for your workouts? Try visiting your dashboard.</p>
      <% if logged_in? %>
        <%= link_to "Go to Dashboard", dashboard_path, class: "text-blue-600 hover:text-blue-800" %>
      <% else %>
        <%= link_to "Log in", login_path, class: "text-blue-600 hover:text-blue-800" %>
      <% end %>
    </div>
  </div>
</div>