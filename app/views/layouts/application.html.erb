<!DOCTYPE html>
<html>
  <head>
    <title>GymFitness App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <header class="bg-white shadow-sm">
      <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
        <div class="flex justify-between items-center">
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center text-xl font-bold text-blue-600" do %>
              <%= image_tag "", alt: "GymFitness Logo", class: "h-8 w-auto mr-2" %>
              <span>GymFitness</span>
            <% end %>
          </div>
          
          <div class="flex items-center space-x-4">
            <% if logged_in? %>
              <%= link_to "Dashboard", dashboard_path, class: "text-gray-600 hover:text-blue-600" %>
              <%= link_to "Workouts", workouts_path, class: "text-gray-600 hover:text-blue-600" %>
              <%= link_to "Goals", goals_path, class: "text-gray-600 hover:text-blue-600" %>
              <%= link_to "Profile", profile_path, class: "text-gray-600 hover:text-blue-600" %>
              <%= button_to "Logout", logout_path, method: :delete, class: "bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700" %>
            <% else %>
              <%= link_to "Login", login_path, class: "text-gray-600 hover:text-blue-600" %>
              <%= link_to "Sign Up", signup_path, class: "bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700" %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
      <% if flash[:notice] %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
          <%= flash[:notice] %>
        </div>
      <% end %>
      
      <% if flash[:error] %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
          <%= flash[:error] %>
        </div>
      <% end %>
      
      <%= yield %>
    </main>
    
    <footer class="bg-gray-800 text-white py-8">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center">
          <p>&copy; <%= Date.today.year %> GymFitness. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>